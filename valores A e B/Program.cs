using System;
using System.Collections.Generic;
using System.Diagnostics.Eventing.Reader;
using System.Linq;
using System.Runtime.Serialization;
using System.Text;
using System.Threading.Tasks;

namespace valores_A_e_B
{
    internal class Program
    {
        /* Faça um algoritimo que leia dois valores A e B, se os dois numeros forem iguais, deverá somar os dois valores,
          caso contrário devera multiplicar os dois. Ao final de qualquer um dos cáuculos deve-se atribuir o resultado
        a uma variável C e imprimir seu valor na tela.  */
        static void Main(string[] args)
        {
            Double  n2, total, soma;
            {
                Console.WriteLine("Digite um valor para A: ");
                Console.ReadLine();
            }
            {
                Console.WriteLine("Digite um valor para B: ");
                Console.ReadLine();
            }
            {
            if Double.Parse(A == B);
            soma = A + B = total;
            Console.WriteLine("C é igual a: " + total);
        }
            {
            else Double.Parse()

            


        }
    }
}
